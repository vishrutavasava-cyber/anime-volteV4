self.addEventListener("install", e => {
  e.waitUntil(
    caches.open("anime-vault").then(cache =>
      cache.addAll([
        "./",
        "./index.html",
        "./style.css",
        "./app.js"
      ])
    )
  );
});